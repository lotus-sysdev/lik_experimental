from django.contrib import admin
from .models import Customer

# @admin.register(Customer)
# # class MyModelAdmin(admin.ModelAdmin):
# #     list_display = ('name', 'email', 'id')  # Specify which fields to display in the list view
# #     search_fields = ('name', 'email','id')  # Add search functionality for these fields
